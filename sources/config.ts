export const defaultVersions = new Map([
    [`npm`, `6.14.5`],
    [`yarn`, `1.22.4`],
    [`pnpm`, `4.14.2`],
]);

export const entries = new Map([
    [`npm`, `npm`],
    [`npx`, `npm`],
    [`pnpm`, `pnpm`],
    [`pnpx`, `pnpm`],
    [`yarn`, `yarn`],
]);
